#ifndef IMG_CRYPT_H
#define IMG_CRYPT_H

// Encrypt an image file: prompts for path (full or project folder), then key.
void encryptImage();

// Decrypt an image file: lists encrypted images, prompts for filename and key.
void decryptImage();

#endif // IMG_CRYPT_H
